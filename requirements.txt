fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
python-multipart>=0.0.9

# Used by PDF/text extraction paths
pdfplumber>=0.11.0
langdetect>=1.0.9

# RAG / embeddings (required if you use the local FAISS path)
sentence-transformers>=3.0.0
faiss-cpu>=1.8.0
numpy>=1.26.0

# HTTP calls to external LLM providers / vectify
requests>=2.32.0

# Optional LLM SDKs (only needed if you set LLM_PROVIDER accordingly)
openai>=0.28.0
google-genai>=0.3.0
